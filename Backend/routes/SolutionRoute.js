const router = require("express").Router();
const controller = require("../controllers/SolutionController");
// router.get("/", controller.GetSolution);
// router.get("/test", controller.testGet);
module.exports = router;
